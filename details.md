# 水温控制系统设计 — 详细方案

---

## 1. 需求分析

| 参数 | 指标                                |
|------|-----------------------------------|
| 控温范围 | 25°C ~ 100°C                         |
| 控温精度 | ±1°C                              |
| 加热方式 | 电阻加热丝（220V/AC 或 12V/DC）           |
| 传感器 | NTC 热敏电阻（MF52 系列，R₂₅=10kΩ，B=3950） |
| 供电方式 | ±12V（运放供电）、+5V（参考电压）              |
| 指示方式 | 绿色 LED（加热中）、红色 LED（停止）            |

---

## 2. 传感器与电桥电路

### 2.1 NTC 热敏电阻选型

选用 **MF52-103-3950** 型 NTC 热敏电阻：
- 25°C 时标称阻值 R₂₅ = 10kΩ
- B 值（材料常数）= 3950K
- 阻值-温度关系（近似公式）：

\[
R_{NTC} = R_{25} \cdot \exp\left[ B \cdot \left( \frac{1}{T} - \frac{1}{T_{25}} \right) \right]
\]

其中 T 为绝对温度（K），T₂₅ = 298.15K。

典型阻值参考：

| 温度 (°C) | 阻值 (kΩ) |
|-----------|-----------|
| 0         | ≈32.5     |
| 25        | 10.0      |
| 50        | ≈3.9      |
| 75        | ≈1.8      |
| 100       | ≈0.95     |

### 2.2 电桥电路

采用惠斯通电桥将 NTC 阻值变化转换为差分电压信号。

```
          +Vcc (+5V)
            │
        ┌───┴───┐
        │       │
       R1      R3(NTC)
        │       │
        ├── Vout+ ─── Vout- ──┤
        │       │
       R2      R4(VR)
        │       │
        └───┴───┘
            │
           GND
```

**元件取值：**
- R1 = R2 = 10kΩ（1% 精密电阻）
- R3 = NTC 热敏电阻
- R4 = 10kΩ 多圈精密电位器（用于调零/校准）

**电桥输出电压：**

\[
V_{out} = V_{cc} \cdot \left( \frac{R_{NTC}}{R_{NTC} + R_1} - \frac{R_4}{R_2 + R_4} \right)
\]

**灵敏度估算：**
- 25°C 时调节 R4 使电桥平衡，Vout ≈ 0V
- 温度升高 → NTC 阻值下降 → Vout 为负向变化（约 −30~−50 mV/°C）
- 温度降低 → NTC 阻值上升 → Vout 为正向变化

---

## 3. 信号调理电路

### 3.1 两级运放放大方案（LM358 / LM324）

#### 第一级：差分放大（减法器，增益 10）

电桥的差分输出经差分放大器转换为单端信号，同时抑制共模电压（电桥平衡时两输出端均为 2.5V DC）。若直接将 Vout+（固定 2.5V DC）用同相放大器放大 10 倍，输出将达 25V 远超电源轨，导致运放饱和失效。

- 电桥 Vout+ → R_inv1 (10kΩ) → 运放反相输入端
- 电桥 Vout− → R_ninv1 (10kΩ) → 运放同相输入端
- 运放反相输入 → R_fb1 (100kΩ) → 运放输出
- 运放同相输入 → R_div1 (100kΩ) → GND

放大倍数：

\[
A_{v1} = \frac{R_{fb1}}{R_{inv1}} = \frac{100k\Omega}{10k\Omega} = 10
\]

（需满足匹配条件：R_div1 / R_ninv1 = R_fb1 / R_inv1 = 10，取 R_ninv1 = 10kΩ，R_div1 = 100kΩ）

#### 第二级：同相放大（增益 10）

第一级输出经 RC 低通滤波后送入第二级同相放大器：

- 滤波后信号 → 运放同相输入端
- 运放反相输入 → R_g2 (1kΩ) → GND
- 运放反相输入 → R_fb2 (9kΩ) → 运放输出

放大倍数：

\[
A_{v2} = 1 + \frac{R_{fb2}}{R_{g2}} = 1 + \frac{9k\Omega}{1k\Omega} = 10
\]

**总放大倍数：** Av_total = Av1 × Av2 = 10 × 10 = 100

**LM358 供电：** 双电源 ±12V（Pin 8 → +12V，Pin 4 → −12V）。差分放大器在电桥不平衡时需输出负电压（温度低于设定值时），单电源供电下负输出被截断为 0V，无法与平衡状态（0V）区分，温控逻辑失效。

**设计目标：**
- 电桥输出 ≈ ±50mV（对应 0~100°C 范围）
- 放大 100 倍后 → ±5V
- 后级接比较器，以 0V 为参考（对应设定温度点）

#### 低通滤波

在两级放大之间插入一阶 RC 低通滤波器：

- R = 15.9kΩ，C = 1μF → f_c = 1/(2πRC) ≈ 10Hz
- 抑制 50Hz 工频干扰

### 3.2 仪表放大器方案（AD620）

如需更高共模抑制比（CMRR > 80dB），可使用集成仪表放大器 AD620：

增益设置电阻：

\[
R_G = \frac{49.4\text{k}\Omega}{G - 1}
\]

取 G = 100 → R_G = 49.4kΩ / 99 ≈ 499Ω（取标准值 499Ω，实际增益 ≈ 100）

---

## 4. 比较控制电路

### 4.1 滞回比较器（施密特触发器）

使用 LM393 开集电极比较器（或 LM358 运放构成比较器）。

```
          +Vcc
            │
            R3
            │
  Vin ──┬──┐│──── Vout
        │    │
        R1   │
        │    │
        └────┴── R2 ── GND
```

**参考电压 Vref 设定：**
- 由 100kΩ 电位器（RV2）从 +5V 分压得到
- 调节范围 0~5V，对应设定温度范围
- Vref 对应电桥经放大后为 0V 时的温度（即目标温度）

> **注：** RV2 需选用 100kΩ（非 10kΩ），否则其内部等效电阻（中心抽头时 R_th ≈ 2.5kΩ）远小于滞后反馈电阻 R5 (100kΩ)，正反馈被严重衰减（分压比仅约 4.8%），滞回过小易引发振荡。

**滞回宽度计算：**

滞回由正反馈电阻 R5 (100kΩ) 和电位器戴维南等效电阻 R_th 共同决定，R6 (1MΩ) 作为弱下拉偏置影响可忽略：

\[
V_H - V_L = \frac{R_{th} \parallel R_6}{R_5 + R_{th} \parallel R_6} \cdot (V_{OH} - V_{OL})
\]

取 R5 = 100kΩ，R6 = 1MΩ，RV2 = 100kΩ（抽头居中时 R_th ≈ 25kΩ），LM393 采用 +12V 单电源供电（V_OH ≈ +12V，V_OL ≈ 0V）：

\[
R_{th} \parallel R_6 = 25k \parallel 1M \approx 24.4k\Omega
\]
\[
V_H - V_L = \frac{24.4k}{100k + 24.4k} \cdot 12 \approx 0.196 \times 12 \approx 2.4\text{V}_{pp}
\]

对应到桥端（放大倍数 100）：约 ±12mV（峰峰值 24mV） → 等效温度约 ±0.25°C（峰峰值约 0.5°C），在 ±1°C 控温精度范围内并提供足够抗干扰裕量。

### 4.2 控制逻辑

LM393 由 +12V 单电源供电（Pin 8 → +12V，Pin 4 → GND），输出经 R7 (10kΩ) 上拉至 +12V：

| 条件 | LM393 输出 (Pin 1) | LED 状态 | 动作 |
|------|-------------------|---------|------|
| 温度 < 设定值 − 滞回 | HIGH（≈+12V） | 绿亮红灭 | 开启加热 |
| 温度 > 设定值 + 滞回 | LOW（≈0V） | 红亮绿灭 | 关闭加热 |
| 温度在滞回窗口内 | 保持上一状态 | — | 保持 |

> **硬件保护提示：** 运放采用 ±12V 双电源后，第二级输出可下探至 −10.5V（温度远低于设定值时）。LM393 输入引脚（Pin 2）允许范围为 −0.3V ~ Vcc−1.5V，长时间输入负压可能损坏芯片。建议在实际硬件中，于运放输出（Pin 7）与比较器输入（Pin 2）之间串联 10kΩ 保护电阻，并加装 BAT54S 等肖特基二极管对 GND 钳位。

---

## 5. 驱动执行电路

### 5.1 三极管驱动继电器

```
        +12V
          │
      ┌───┤
      │ 继电器线圈
      │   │
      │   └───┐
      │       │
      │      ｜ 1N4007
      │       │
      └───┐   │
          │   │
          R   │
          │   │
      ┌───┤   │
      │   │   │
      │  NPN  │
      │   │   │
      │   │   │
      └───┴───┘
          │
         GND
```

**元件参数：**
- NPN 三极管：**S8050** 或 **2N2222**（I_C(max) ≥ 500mA）
- 基极电阻 R = 1kΩ（限流，基极电流 ≈ 1mA）
- 继电器：**SRD-05VDC-SL-C**（5V 线圈，10A 触点，或选用 12V 线圈版本）
- 续流二极管：**1N4007**（保护三极管免受反电动势击穿）

### 5.2 加热器控制

继电器触点控制 220V/AC 加热器（如电热棒、加热丝）：

```
      L(火线)
        │
     继电器常开触点
        │
      ┌──┤
      │ 加热器(220V)
      │  │
      └──┤
        │
      N(零线)
```

### 5.3 光耦隔离（可选）

如需强弱电完全隔离，在比较器输出与三极管基极之间加入 **PC817** 光耦：

- 光耦输入端限流电阻 470Ω（接比较器输出）
- 光耦输出端上拉电阻 10kΩ 至 +12V，接三极管基极

---

## 6. 辅助电路

### 6.1 电源电路

#### ±12V 供电

方案一：**双绕组变压器 + 整流桥 + 三端稳压**

```
220V/AC → 变压器(12V-0-12V) → 桥式整流(1N4007×4)
    → 滤波电容(1000μF/25V×2) → 7812(+12V) + 7912(−12V)
    → 输出电容(100μF + 0.1μF)
```

方案二（简易）：**单电源 + 虚拟地**

- 用 +12V 单电源供电，通过电阻分压 + 运放跟随器生成虚拟地（+6V）
- 将虚拟地作为信号参考点

#### +5V 参考电压

- 从 +12V 经 **7805** 稳压得到 +5V
- 为电桥、参考电位器供电

### 6.2 LED 指示电路

LM393 为开集电极输出，需上拉电阻 R7 (10kΩ) 至 +12V（Pin 1 → R7 → +12V）。

| LED | 颜色 | 串阻 | 接线方式 | 驱动条件 |
|-----|------|------|----------|----------|
| 加热指示 | 绿色 | 1kΩ (R8) | Pin 1 → 绿色 LED 阳极 → 阴极 → R8(1kΩ) → GND | 比较器输出高电平（加热中） |
| 停止指示 | 红色 | 1kΩ (R9) | +12V → R9(1kΩ) → 红色 LED 阳极 → 阴极 → Pin 1 | 比较器输出低电平（停止加热） |

> **注：** 绿色 LED 亮时电流路径为 +12V → R7(10kΩ) → Pin 1 → LED → R8(1kΩ) → GND，LED 电流约 0.91mA，亮度偏暗。如需提高亮度，可将 R7 从 10kΩ 改为 1kΩ（此时 LED 电流约 5mA），但需确认 LM393 灌电流能力（典型约 16mA）是否满足 R7 灌电流 + 红色 LED 电流的总和。

### 6.3 过温保护（可选）

在加热器回路中串联一个 **KSD-01F 型 105°C 常闭温控开关**，紧贴加热容器壁。

- 正常时导通，超温时断开，切断加热器电源
- 温度回落后自动恢复

---

## 7. 仿真验证（Multisim / Proteus）

### 7.1 仿真步骤

1. **搭建 NTC 等效模型**
   - 用可变电阻器代替 NTC，阻值范围 1kΩ~33kΩ
   - 扫描阻值 → 模拟温度变化

2. **电桥电路仿真**
   - 调节 R4 使 25°C 时电桥输出 ≈ 0V
   - 测量不同阻值下的电桥输出电压

3. **放大电路仿真**
   - 验证总放大倍数 ≈ 100
   - 观察输出波形是否饱和（应在 ±10V 以内）

4. **比较器仿真**
   - 调节 Vref 设定目标温度
   - 温度扫描分析（DC Sweep）验证比较器翻转点

5. **全链路仿真**
   - 连接电桥 → 放大 → 比较 → LED 指示
   - 验证控温逻辑正确

### 7.2 关键测试点

| 测试点 | 信号 | 正常范围 |
|--------|------|----------|
| 电桥差分输出 (Vout+ − Vout−) | 差分电压 | 0V（平衡，25°C）~ ±55mV/°C |
| 第一级输出 (运放 A 输出，滤波前) | 单端电压 | ±0.57V/°C（平衡时 0V） |
| 第二级输出 (运放 B 输出) | 单端电压 | ±5.7V/°C（平衡时 0V） |
| 比较器输出 (LM393 Pin 1) | 数字电平 | HIGH ≈ +12V / LOW ≈ 0V |

> **注：** ±1°C 精度范围内放大后信号约 ±5.7V，在 ±12V 供电的 LM358 输出范围内（典型 ±10.5V），不会饱和。

### 7.3 仿真波形截图

保存以下截图用于设计报告：
- 电桥输出随温度变化曲线
- 放大电路输出波形
- 比较器滞回特性曲线（传输特性）
- 全电路温度扫描结果

---

## 8. PCB 设计（可选 / Altium Designer 或 立创EDA）

### 8.1 布局要点

- **分区布局**：电源区 → 模拟信号区 → 功率驱动区 → 强电区
- **电源与地**：电源走线加宽（≥ 40mil），信号地与大电流地单点连接
- **模拟信号**：远离继电器、变压器等电磁干扰源
- **散热**：7812/7912 预留散热片位置

### 8.2 布线要点

| 类型 | 线宽 | 间距 |
|------|------|------|
| 信号线 | 10~12mil | ≥ 10mil |
| 电源线 | 40~60mil | ≥ 15mil |
| 强电线 | 80~100mil | ≥ 40mil |

### 8.3 元件封装

| 元件 | 封装 |
|------|------|
| 电阻 (1/4W) | AXIAL-0.4 |
| 电解电容 | RB5-10.5 |
| 瓷片电容 | RAD-0.1 |
| 运放 LM358/LM324 | DIP-8 |
| 三极管 S8050 | TO-92 |
| 继电器 | 5V 继电器座 |
| 整流桥 | DIP-4 |
| 7805/7812/7912 | TO-220 |
| 电位器 | 3296W |

---

## 9. 设计报告建议章节

| 章节 | 内容 |
|------|------|
| 1. 课题任务与要求 | 设计指标、功能说明 |
| 2. 方案论证 | 传感器选型、电路结构对比 |
| 3. 电路设计 | 各模块原理与参数计算 |
| 4. 仿真分析 | 仿真电路图、波形、结果分析 |
| 5. 实物调试（可选） | 焊接、调试过程与问题 |
| 6. 总结与心得体会 | 设计收获与不足 |

---

> **注：** 本方案以模拟电子技术为核心，所有关键模块（传感器、信号调理、比较控制、驱动执行）均采用运放、三极管、电阻电容等模拟器件实现，符合本科《模拟电子技术》课程设计要求。
