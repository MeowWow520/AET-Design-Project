# 水温控制系统设计 — 详细方案

---

## 1. 需求分析

| 参数 | 指标                                |
|------|-----------------------------------|
| 控温范围 | 25°C ~ 100°C                         |
| 控温精度 | ±1°C                              |
| 加热方式 | 电阻加热丝（220V/AC 或 12V/DC）           |
| 传感器 | NTC 热敏电阻（MF52 系列，R₂₅=10kΩ，B=3950） |
| 供电方式 | ±12V（运放供电）、+5V（参考电压）              |
| 指示方式 | 绿色 LED（加热中）、红色 LED（停止）            |

---

## 2. 传感器与电桥电路

### 2.1 NTC 热敏电阻选型

选用 **MF52-103-3950** 型 NTC 热敏电阻：
- 25°C 时标称阻值 R₂₅ = 10kΩ
- B 值（材料常数）= 3950K
- 阻值-温度关系（近似公式）：

\[
R_{NTC} = R_{25} \cdot \exp\left[ B \cdot \left( \frac{1}{T} - \frac{1}{T_{25}} \right) \right]
\]

其中 T 为绝对温度（K），T₂₅ = 298.15K。

典型阻值参考：

| 温度 (°C) | 阻值 (kΩ) |
|-----------|-----------|
| 0         | ≈32.5     |
| 25        | 10.0      |
| 50        | ≈3.9      |
| 75        | ≈1.8      |
| 100       | ≈0.95     |

### 2.2 电桥电路

采用惠斯通电桥将 NTC 阻值变化转换为差分电压信号。

```
          +Vcc (+5V)
            │
        ┌───┴───┐
        │       │
       R1      R3(NTC)
        │       │
        ├── Vout+ ─── Vout- ──┤
        │       │
       R2      R4(VR)
        │       │
        └───┴───┘
            │
           GND
```

**元件取值：**
- R1 = R2 = 10kΩ（1% 精密电阻）
- R3 = NTC 热敏电阻
- R4 = 10kΩ 多圈精密电位器（用于调零/校准）

**电桥输出电压：**

\[
V_{out} = V_{cc} \cdot \left( \frac{R_{NTC}}{R_{NTC} + R_1} - \frac{R_4}{R_2 + R_4} \right)
\]

**灵敏度估算：**
- 25°C 时调节 R4 使电桥平衡，Vout ≈ 0V
- 温度升高 → NTC 阻值下降 → Vout 为负向变化（约 −30~−50 mV/°C）
- 温度降低 → NTC 阻值上升 → Vout 为正向变化

---

## 3. 信号调理电路

### 3.1 两级运放放大方案（LM358 / LM324）

#### 第一级：同相放大

放大倍数：

\[
A_{v1} = 1 + \frac{R_{f1}}{R_{g1}}
\]

取 Rg1 = 1kΩ，Rf1 = 9kΩ，则 Av1 = 10。

#### 第二级：差分转单端（减法器）

放大倍数：

\[
V_{out} = A_{v2} \cdot (V_{in+} - V_{in-})
\]

取 Rf2 = 100kΩ，Rg2 = 10kΩ，则 Av2 = 10。

**总放大倍数：** Av_total = Av1 × Av2 = 100

**设计目标：**
- 电桥输出 ≈ ±50mV（对应 0~100°C 范围）
- 放大 100 倍后 → ±5V
- 后级接比较器，以 0V 为参考（对应设定温度点）

#### 低通滤波

在两级放大之间插入一阶 RC 低通滤波器：

- R = 15.9kΩ，C = 1μF → f_c = 1/(2πRC) ≈ 10Hz
- 抑制 50Hz 工频干扰

### 3.2 仪表放大器方案（AD620）

如需更高共模抑制比（CMRR > 80dB），可使用集成仪表放大器 AD620：

增益设置电阻：

\[
R_G = \frac{49.4\text{k}\Omega}{G - 1}
\]

取 G = 100 → R_G = 49.4kΩ / 99 ≈ 499Ω（取标准值 499Ω，实际增益 ≈ 100）

---

## 4. 比较控制电路

### 4.1 滞回比较器（施密特触发器）

使用 LM393 开集电极比较器（或 LM358 运放构成比较器）。

```
          +Vcc
            │
            R3
            │
  Vin ──┬──┐│──── Vout
        │    │
        R1   │
        │    │
        └────┴── R2 ── GND
```

**参考电压 Vref 设定：**
- 由 10kΩ 电位器从 +5V 分压得到
- 调节范围 0~5V，对应设定温度范围
- Vref 对应电桥经放大后为 0V 时的温度（即目标温度）

**滞回宽度计算：**

\[
V_H - V_L = \frac{R_1}{R_1 + R_2} \cdot (V_{OH} - V_{OL})
\]

取 R1 = 100kΩ，R2 = 1MΩ，Vcc = ±12V：

\[
V_H - V_L = \frac{100k}{100k + 1M} \cdot (12 - (-12)) \approx 0.109 \times 24 \approx 2.6\text{V}_{pp}
\]

对应到输入端（放大倍数 100）：约 ±13mV → 等效温度 ±0.3~0.4°C。

### 4.2 控制逻辑

| 条件 | 输出 | 动作 |
|------|------|------|
| 温度 < 设定值 − 滞回 | 高电平 (+12V) | 开启加热 |
| 温度 > 设定值 + 滞回 | 低电平 (−12V / GND) | 关闭加热 |

---

## 5. 驱动执行电路

### 5.1 三极管驱动继电器

```
        +12V
          │
      ┌───┤
      │ 继电器线圈
      │   │
      │   └───┐
      │       │
      │      ｜ 1N4007
      │       │
      └───┐   │
          │   │
          R   │
          │   │
      ┌───┤   │
      │   │   │
      │  NPN  │
      │   │   │
      │   │   │
      └───┴───┘
          │
         GND
```

**元件参数：**
- NPN 三极管：**S8050** 或 **2N2222**（I_C(max) ≥ 500mA）
- 基极电阻 R = 1kΩ（限流，基极电流 ≈ 1mA）
- 继电器：**SRD-05VDC-SL-C**（5V 线圈，10A 触点，或选用 12V 线圈版本）
- 续流二极管：**1N4007**（保护三极管免受反电动势击穿）

### 5.2 加热器控制

继电器触点控制 220V/AC 加热器（如电热棒、加热丝）：

```
      L(火线)
        │
     继电器常开触点
        │
      ┌──┤
      │ 加热器(220V)
      │  │
      └──┤
        │
      N(零线)
```

### 5.3 光耦隔离（可选）

如需强弱电完全隔离，在比较器输出与三极管基极之间加入 **PC817** 光耦：

- 光耦输入端限流电阻 470Ω（接比较器输出）
- 光耦输出端上拉电阻 10kΩ 至 +12V，接三极管基极

---

## 6. 辅助电路

### 6.1 电源电路

#### ±12V 供电

方案一：**双绕组变压器 + 整流桥 + 三端稳压**

```
220V/AC → 变压器(12V-0-12V) → 桥式整流(1N4007×4)
    → 滤波电容(1000μF/25V×2) → 7812(+12V) + 7912(−12V)
    → 输出电容(100μF + 0.1μF)
```

方案二（简易）：**单电源 + 虚拟地**

- 用 +12V 单电源供电，通过电阻分压 + 运放跟随器生成虚拟地（+6V）
- 将虚拟地作为信号参考点

#### +5V 参考电压

- 从 +12V 经 **7805** 稳压得到 +5V
- 为电桥、参考电位器供电

### 6.2 LED 指示电路

| LED | 颜色 | 串阻 | 驱动条件 |
|-----|------|------|----------|
| 加热指示 | 绿色 | 1kΩ | 比较器输出高电平（加热中） |
| 停止指示 | 红色 | 1kΩ | 比较器输出低电平（停止加热） |

红色 LED 可通过 PNP 三极管反相驱动，或使用比较器反相输出端。

### 6.3 过温保护（可选）

在加热器回路中串联一个 **KSD-01F 型 105°C 常闭温控开关**，紧贴加热容器壁。

- 正常时导通，超温时断开，切断加热器电源
- 温度回落后自动恢复

---

## 7. 仿真验证（Multisim / Proteus）

### 7.1 仿真步骤

1. **搭建 NTC 等效模型**
   - 用可变电阻器代替 NTC，阻值范围 1kΩ~33kΩ
   - 扫描阻值 → 模拟温度变化

2. **电桥电路仿真**
   - 调节 R4 使 25°C 时电桥输出 ≈ 0V
   - 测量不同阻值下的电桥输出电压

3. **放大电路仿真**
   - 验证总放大倍数 ≈ 100
   - 观察输出波形是否饱和（应在 ±10V 以内）

4. **比较器仿真**
   - 调节 Vref 设定目标温度
   - 温度扫描分析（DC Sweep）验证比较器翻转点

5. **全链路仿真**
   - 连接电桥 → 放大 → 比较 → LED 指示
   - 验证控温逻辑正确

### 7.2 关键测试点

| 测试点 | 信号 | 正常范围 |
|--------|------|----------|
| 电桥输出 | 差分电压 | ±50mV |
| 放大后输出 | 单端电压 | 0~±5V |
| 比较器输出 | 数字电平 | ±12V 或 GND/+12V |

### 7.3 仿真波形截图

保存以下截图用于设计报告：
- 电桥输出随温度变化曲线
- 放大电路输出波形
- 比较器滞回特性曲线（传输特性）
- 全电路温度扫描结果

---

## 8. PCB 设计（可选 / Altium Designer 或 立创EDA）

### 8.1 布局要点

- **分区布局**：电源区 → 模拟信号区 → 功率驱动区 → 强电区
- **电源与地**：电源走线加宽（≥ 40mil），信号地与大电流地单点连接
- **模拟信号**：远离继电器、变压器等电磁干扰源
- **散热**：7812/7912 预留散热片位置

### 8.2 布线要点

| 类型 | 线宽 | 间距 |
|------|------|------|
| 信号线 | 10~12mil | ≥ 10mil |
| 电源线 | 40~60mil | ≥ 15mil |
| 强电线 | 80~100mil | ≥ 40mil |

### 8.3 元件封装

| 元件 | 封装 |
|------|------|
| 电阻 (1/4W) | AXIAL-0.4 |
| 电解电容 | RB5-10.5 |
| 瓷片电容 | RAD-0.1 |
| 运放 LM358/LM324 | DIP-8 |
| 三极管 S8050 | TO-92 |
| 继电器 | 5V 继电器座 |
| 整流桥 | DIP-4 |
| 7805/7812/7912 | TO-220 |
| 电位器 | 3296W |

---

## 9. 设计报告建议章节

| 章节 | 内容 |
|------|------|
| 1. 课题任务与要求 | 设计指标、功能说明 |
| 2. 方案论证 | 传感器选型、电路结构对比 |
| 3. 电路设计 | 各模块原理与参数计算 |
| 4. 仿真分析 | 仿真电路图、波形、结果分析 |
| 5. 实物调试（可选） | 焊接、调试过程与问题 |
| 6. 总结与心得体会 | 设计收获与不足 |

---

> **注：** 本方案以模拟电子技术为核心，所有关键模块（传感器、信号调理、比较控制、驱动执行）均采用运放、三极管、电阻电容等模拟器件实现，符合本科《模拟电子技术》课程设计要求。
