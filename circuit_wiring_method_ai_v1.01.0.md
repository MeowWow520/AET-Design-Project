# Proteus 仿真接线方法
> 版本 ver.1.01.0 (AI 校验修正版)

## 前言

本文件描述了本课题设计在 Proteus 仿真软件中的接线方法。包括选取的元器件型号和相关参数。本文件分模块描述各部分电路。本版本基于 `circuit_wiring_method_v1.00.0.md`，对所有元器件的参数进行了逐一验证，对错误接线进行了更正。

---

## 第一部分：惠斯通电桥

### 1、使用的元器件

|      元件      |  符号   |       型号       |            参数            | Proteus 元件库描述 |
|:------------:|:-----:|:--------------:|:------------------------:|:-------------:|
|      电阻      | R1，R2 |       --       |      10kΩ（1% 精密电阻）      |      RES      |
| 10kΩ 多圈精密电位器 |  RV1  |   POT-HG (推荐)   |           10kΩ           |    POT-HG     |
|     热敏电阻     |  RT1  | MF52-103-3950  | R₂₅=10kΩ, B=3950 (NTC型) | NTC (或 POT 代替) |

### 2、接线方式

桥臂组成（从上到下，从左到右）：

- **左臂**： R1 (10kΩ) 上端接 **+5V**，下端接 R2 (10kΩ)；R2 (10kΩ) 下端接 **GND**
- **右臂**： RT1 (NTC 热敏电阻) 上端接 **+5V**，下端接 RV1 (10kΩ 电位器)；RV1 下端接 **GND**

差分输出（两个 Vout）：

- **Vout+**（左臂中点）：R1 与 R2 的连接点
- **Vout−**（右臂中点）：RT1 (NTC) 与 RV1 (电位器) 的连接点

> **验证结论：** 本部分接线与 `details.md` 第 2.2 节完全一致，元器件参数合理，无需更正。+5V 供电与 `details.md` 第 1 节"供电方式"匹配。灵敏度约 −50~−57mV/°C（25°C 附近），符合 ±50mV 设计预期。

---

## 第二部分：运算放大电路

### 1、使用的元器件

|  元件   |                  符号                   |       型号       |           参数            |  Proteus 元件库描述   |
|:-----:|:-------------------------------------:|:--------------:|:-----------------------:|:---------------:|
| 运算放大器 |              U1:A，U1:B               | LM358(NAT0A)   |            --            |  LM358(NAT0A)   |
|  电阻   | R_inv1（原 RG1 对应），R_fb1（原 RF1 对应），R_f，R_ninv1（原 RG2 对应），R_div1（原 R4 对应），R_g2（原 RG2' 对应），R_fb2（原 RF2 对应） |       --       | 见下表（参数列） |       --       |
|  电容   |                  C1                   |       --       |           1μF           |       --        |

各电阻参数一览：

|    符号    |  阻值   |                       用途                       |   原版本标识   |
|:--------:|:-----:|:----------------------------------------------:|:--------:|
| R_inv1   | 10kΩ  |          第一级差放：Vout+ → Pin 2（反相输入电阻）           |   RG1    |
| R_fb1    | 100kΩ |            第一级差放：Pin 2 → Pin 1（反馈电阻）            |   RF1    |
| R_ninv1  | 10kΩ  |          第一级差放：Vout− → Pin 3（同相输入电阻）            |   RG2    |
| R_div1   | 100kΩ |             第一级差放：Pin 3 → GND（分压电阻）             |    R4    |
| R_f      | 15.9kΩ |               RC 低通滤波（串联电阻）                | R3（原标识） |
| R_g2     | 1kΩ   |         第二级同相放大：Pin 6 → GND（增益设置电阻）          |  RG2'（原标识） |
| R_fb2    | 9kΩ   |          第二级同相放大：Pin 6 → Pin 7（反馈电阻）          |  RF2（原标识） |

LM358(NAT0A) 引脚标号：

```text
Pin 1 -- Output A           (运放 A 输出)
Pin 2 -- Inverting Input A  (运放 A 反相输入)
Pin 3 -- Non-Inverting A    (运放 A 同相输入)
Pin 4 -- VEE (负电源, −12V)  
Pin 5 -- Non-Inverting B    (运放 B 同相输入)
Pin 6 -- Inverting Input B  (运放 B 反相输入)
Pin 7 -- Output B           (运放 B 输出)
Pin 8 -- VCC (正电源, +12V)
```

### 2、接线方式

> **注：** 以下为更正后的正确接线。原版本接线存在拓扑错误，详情见下方"3、更正原因"。

**第一级（差分放大，增益 10）—— 运放 A：**

- 电桥 Vout+ → R_inv1 (10kΩ) → Pin 2（运放 A 反相输入端）
- Pin 2 → R_fb1 (100kΩ) → Pin 1（运放 A 输出）
- 电桥 Vout− → R_ninv1 (10kΩ) → Pin 3（运放 A 同相输入端）
- Pin 3 → R_div1 (100kΩ) → GND
- Pin 1（运放 A 输出） → R_f (15.9kΩ) → C1 (1μF 电容) → GND（RC 低通滤波，fc ≈ 10Hz）
- C1 上端（滤波后信号） → Pin 5（运放 B 同相输入），记为 **V_stage1**

增益：A_v1 = R_fb1 / R_inv1 = 100kΩ / 10kΩ = **10**

**第二级（同相放大，增益 10）—— 运放 B：**

- Pin 6（运放 B 反相输入） → R_g2 (1kΩ) → GND
- Pin 6 → R_fb2 (9kΩ) → Pin 7（运放 B 输出）
- Pin 7（运放 B 输出） → 送至 LM393 比较器 Pin 2（反相输入）

增益：A_v2 = 1 + R_fb2 / R_g2 = 1 + 9kΩ / 1kΩ = **10**

**总放大倍数：** A_v_total = A_v1 × A_v2 = 10 × 10 = **100**

**供电：** Pin 8 → **+12V**，Pin 4 → **−12V**（双电源供电）

---

### 3、更正原因

#### 3.1 第一级拓扑错误（关键）

原版本将第一级设计为"同相放大器"：Vout+ 直接接 Pin 3，RG1(1kΩ) 和 RF1(9kΩ) 设置增益为 10。

**问题：** Vout+ 是电桥左臂中点，在 R1 = R2 = 10kΩ 下固定为 2.5V DC（与温度无关）。将此直流电平放大 10 倍，得到 25V，远超 LM358 在 +12V 单电源供电下的最大输出能力（约 +10.5V），运放输出**饱和**，整个放大链路失效。

**修正：** 将第一级改为**差分放大器**（减法器），同时引入 Vout+ 和 Vout−。两者在电桥平衡时（25°C）的直流电平均为 2.5V，共模电压被差分放大器抑制，平衡输出为 0V，不会饱和。差分放大器仅放大两个输入之间的差值（即实际温度信号）。

增益 = R_fb1 / R_inv1 = 100kΩ / 10kΩ = 10，且需满足 R_div1 / R_ninv1 = R_fb1 / R_inv1 = 10，取 R_ninv1 = 10kΩ，R_div1 = 100kΩ。

#### 3.2 第二级电阻值修正

原版本第二级为"减法器"：RG2 = 10kΩ，R4 = 10kΩ，RG2' = 10kΩ，RF2 = 100kΩ。该配置存在两个问题：

1. **电阻匹配错误：** 原 R4 = 10kΩ 使 R4/RG2 = 1，不等于 RF2/RG2' = 10，导致减法器不平衡，同相端和反相端增益不一致（分别为 5.5 和 10）。
2. **功能冗余：** 原第一级饱和后，第二级无论怎么接都无效。

修正后，原"减法器"改为**同相放大级**，继续提供增益 10。所需电阻：R_g2 = 1kΩ（原 RG2' 位置），R_fb2 = 9kΩ（原 RF2 位置），增益 = 1 + 9kΩ/1kΩ = 10，与 `details.md` 3.1 节一致。

#### 3.3 运放供电修正

原版本标注"Pin 4 → GND（负电源）"为单电源供电。差分放大器在电桥不平衡时需输出负电压（温度低于设定值时），单电源下负输出被截断为 0V，无法与平衡状态（0V）区分，温控逻辑失效。

**修正：** Pin 4 → **−12V**，与 `details.md` 第 1 节"±12V（运放供电）"一致。

> **注意：** LM358 改为 ±12V 双电源后，输出可摆动 −10.5V ~ +10.5V（典型值）。第二级输出超出 ±10.5V 时饱和，但 ±1°C 精度范围内（输出约 ±5.7V）不会饱和，不影响温控功能。

#### 3.4 RC 低通滤波器参数验证

R_f = 15.9kΩ，C1 = 1μF → f_c = 1/(2πRC) ≈ 10Hz，有效抑制 50Hz 工频干扰。参数合理，与 `details.md` 3.1 节一致。不作修改。

---

## 第三部分：比较电路

### 1、使用的元器件

|  元件   |        符号         |      型号      |           参数           | Proteus 元件库描述 |
|:-----:|:-----------------:|:------------:|:----------------------:|:-------------:|
|  比较器  |        U2         | LM393(NAT0A) |           --           |  LM393(NAT0A)  |
| LED 灯 |      D1，D2        |      --      |    绿色 (Vf ≈ 2.0V) / 红色 (Vf ≈ 1.8V)     | LED-GREEN / LED-RED |
|  电阻   | R5，R6，R7，R8，R9  |      --      |        见下表（参数列）         |      --       |
|  电位器  |        RV2        |   POT-HG     |         100kΩ          |    POT-HG     |

各电阻参数一览：

|  符号  |  阻值   |                       用途                       |
|:----:|:-----:|:----------------------------------------------:|
|  R5  | 100kΩ |    滞回正反馈：Pin 3 → Pin 1（施密特触发器）       |
|  R6  | 1MΩ   |     弱下拉偏置：Pin 3 → GND（防浮空，可选保留）        |
|  R7  | 10kΩ  |           输出上拉：Pin 1 → +12V               |
|  R8  | 1kΩ   |      绿色 LED 限流（加热指示）                        |
|  R9  | 1kΩ   |      红色 LED 限流（停止指示）                        |
| RV2  | 100kΩ |        参考电压调节（两端接 +5V 和 GND）               |

LM393 引脚标号：

```text
Pin 1 -- Output A         (比较器 A 输出，开集电极)
Pin 2 -- Inverting A (−)  (比较器 A 反相输入)
Pin 3 -- Non-Inverting A  (比较器 A 同相输入)
Pin 4 -- GND
Pin 5 -- Non-Inverting B  (未使用)
Pin 6 -- Inverting B      (未使用)
Pin 7 -- Output B         (未使用)
Pin 8 -- VCC              (+12V)
```

### 2、接线方式

**一、供电：** Pin 8 → **+12V**，Pin 4 → **GND**

**二、比较输入：**

- Pin 2（反相输入 −） → 接 运放 B 输出（Pin 7），即放大后的温度信号
- Pin 3（同相输入 +） → 接 Vref 电位器 RV2 中抽头（电位器两端接 **+5V** 和 **GND**，调节得到目标温度对应的参考电压）

**三、滞回反馈（施密特触发器）：**

- Pin 3 → R5 (100kΩ) → Pin 1（输出）（正反馈，产生滞回）
- Pin 3 → R6 (1MΩ) → GND（弱下拉偏置，防止电位器抽头接触不良时 Pin 3 浮空）

**四、输出上拉：** LM393 是开集电极输出，需要上拉电阻才能输出高电平：

- Pin 1 → R7 (10kΩ) → +12V

**五、LED 指示：**

- **绿色 LED（加热中，输出高电平时亮）**：
  Pin 1 → 绿色 LED 阳极 → 绿色 LED 阴极 → R8 (1kΩ) → GND
- **红色 LED（停止，输出低电平时亮）**：
  +12V → R9 (1kΩ) → 红色 LED 阳极 → 红色 LED 阴极 → Pin 1

---

### 3、更正原因

#### 3.1 电阻 R7 命名冲突修正

原版本中 R7 被重复使用：一处为上拉电阻（10kΩ → +12V），另一处为绿色 LED 限流电阻（1kΩ → GND）。现明确：R7 = 10kΩ（上拉），R8 = 1kΩ（绿色 LED 限流），R9 = 1kΩ（红色 LED 限流）。

#### 3.2 电位器 RV2 阻值修正

原版本未指定 RV2 阻值。若 RV2 = 10kΩ，其内部等效电阻（中心抽头时 R_th ≈ 5kΩ）远小于 R5 (100kΩ)，滞回正反馈被严重衰减——反馈分压仅约 4.8%，等效滞回电压约 0.58V，对应桥端约 5.8mV（≈0.12°C），滞回过小易引发振荡。

**修正：** RV2 改为 **100kΩ**。当抽头居中时 R_th ≈ 25kΩ，反馈分压约 **20%**，滞回电压约 **2.4V**，折算到桥端约 **24mV（≈0.5°C）**，符合 ±1°C 控温精度且提供足够抗干扰裕量，与 `details.md` 4.1 节滞回设计值（约 2.6V_pp）接近。

#### 3.3 绿色 LED 驱动电流提示

绿色 LED 亮起时，电流路径为：+12V → R7(10kΩ 上拉) → Pin 1 → 绿色 LED → R8(1kΩ) → GND。LED 电流 ≈ (12−2)/(10k+1k) ≈ **0.91mA**，亮度偏暗。

如需提高绿色 LED 亮度，可将 R7 从 10kΩ 改为 **1kΩ**（此时 LED 电流约 5mA）。代价是 LM393 输出低电平时流过 R7 的灌电流增至 12mA，加上红色 LED 的 10mA，总计约 22mA（LM393 典型灌电流能力约 16mA，需确认具体型号数据手册）。若不可行，可另增一个 NPN 三极管（如 2N2222）缓冲驱动绿色 LED，或用 LM393 的第二路比较器（Pin 5−7）做反相驱动。

#### 3.4 R6 (1MΩ) 评估

R6 = 1MΩ 至 GND 作为弱下拉偏置，其阻抗远大于 RV2 的内部电阻，对滞回计算影响可忽略。其主要作用是防止电位器抽头接触不良时 Pin 3 浮空。**可保留，不作修改。**

#### 3.5 LM393 输入负压保护提示

运放改用 ±12V 双电源后，第二级输出可下探至 −10.5V（温度低于设定值时）。LM393 输入引脚（Pin 2）的允许范围为 −0.3V ~ Vcc−1.5V（典型），长时间输入负压可能损坏芯片。建议在实际硬件中，于运放输出（Pin 7）与比较器输入（Pin 2）之间串联一个 10kΩ 保护电阻，并加装 BAT54S 等肖特基二极管对 GND 钳位。Proteus 仿真中可暂忽略此问题。

#### 3.6 控制逻辑验证

|      运放输出 (Pin 7)     | LM393 比较 | 输出 (Pin 1) | LED 状态 |     动作     |
|:----------------------:|:--------:|:---------:|:------:|:----------:|
| 负（温度 < 设定值 − 滞回） | Pin 2 < Pin 3 |    HIGH   | 绿亮红灭  | **开启加热** |
| 0V 附近（温度 ≈ 设定值）  | 滞回窗口内  |   保持上状态   |    —   |    保持     |
| 正（温度 > 设定值 + 滞回） | Pin 2 > Pin 3 |    LOW    | 红亮绿灭  | **停止加热** |

---

## 附录：关键测试点参考值

| 测试点 | 信号 | 正常范围 |
|--------|------|----------|
| 电桥差分输出 (Vout+ − Vout−) | 差分电压 | 0V（平衡）~ ±55mV/°C |
| 第一级输出 (Pin 1，滤波前) | 单端电压 | ±0.57V/°C（平衡时 0V） |
| 第二级输出 (Pin 7) | 单端电压 | ±5.7V/°C（平衡时 0V） |
| 比较器输出 (Pin 1) | 数字电平 | HIGH ≈ +12V / LOW ≈ 0V |

> **注：** 以上为 25°C 平衡点附近的标称值。±1°C 精度范围内放大后信号约 ±5.7V，在 ±12V 供电的 LM358 输出范围内（±10.5V typ）。

---

> 本版本基于 `circuit_wiring_method_v1.00.0.md` 校验修正生成。如需与原始版本对照，请查阅原文件。
