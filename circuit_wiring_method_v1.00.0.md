# Proteus 仿真接线方法
> 版本 ver.1.00.0
## 前言

本文件描述了本课题设计在 Proteus 仿真软件中的接线方法。包括选取的元器件型号和相关参数。本文件分模块描述各部分电路。


### 第一部分：惠斯通电桥
1、使用的元器件

|      元件      |  符号   | 型号 | 参数 | Proteus 元件库描述 |
|:------------:|:-----:|:--:|:--:|:-------------:|
|      电阻      | R1，R2 | -- | -- |      --       |
| 10kΩ 多圈精密电位器 |  RV1  | -- | -- |      --       |
|     热敏电阻     |  RT1  | -- | -- |      --       |

2、接线方式

桥臂组成（从上到下，从左到右）：
- 左臂： R1(10kΩ) 上端接 +5V（存疑），下端接 R2(10kΩ) ；R2(10kΩ) 下端接 GND
- 右臂： RT1 (NTC 热敏电阻) 上端接 +5V（存疑），下端接 RV1 (10kΩ 电位器)；RV1 下端接 GND

差分输出（两个 Vout）：
- *Vout+*（左臂中点）：R1 与 R2 的连接点
- *Vout−*（右臂中点）：RT1 (NTC) 与 RV1 (电位器) 的连接点

### 第二部分：运算放大电路
**1、使用的元器件**

|  元件   |             符号             |      型号      | 参数 | Proteus 元件库描述 |
|:-----:|:--------------------------:|:------------:|:--:|:-------------:|
| 运算放大器 |         U1:A，U1:B          | LM358(NAT0A) | -- |      --       |
|  电阻   | RG1，RF1，R3，RG2，RG2'，R4，RF2 |      --      | -- |      --       |
|  电容   |             C1             |      --      | -- |      --       | 

LM358(NAT0A) 引脚标号：
```text
Pin 1 -- Output A           (运放 A 输出)
Pin 2 -- Inverting Input A  (运放 A 反相输入)
Pin 3 -- Non-Inverting A    (运放 A 同相输入)
Pin 4 -- GND                (负电源)
Pin 5 -- Non-Inverting B    (运放 B 同相输入)
Pin 6 -- Inverting Input B  (运放 B 反相输入)
Pin 7 -- Output B           (运放 B 输出)
Pin 8 -- VCC                (正电源，+12V)
```

**2、接线方式**

第一级（同相放大，增益 10）— 运放 A：
- 电桥 Vout+ --> 直接接 Pin 3（运放 A 同相输入端）
- Pin 2（运放 A 反相输入端） --> RG1(1kΩ) --> GND，同时 Pin 2 --> RF1(9kΩ) --> Pin 1
- Pin 1（运放 A 输出） --> R3(15.9kΩ) --> C1(1μF 电容) --> GND（RC 低通滤波）--> 输出给第二级，记为 V_stage1

第二级（减法器，增益 10）— 运放 B：
- 电桥 Vout− --> RG2(10kΩ) --> Pin 5（运放 B 同相输入端）；同时 Pin 5 --> R4(10kΩ) --> GND
- 第一级输出 V_stage1 --> RG2'(10kΩ) --> Pin 6（运放 B 反相输入端）；同时 Pin 6 --> RF2(100kΩ) --> Pin 7（运放 B 输出）
- Pin 7（运放 B 输出） --> 送比较器 LM393 做滞回比较 

供电： Pin 8 --> +12V，Pin 4 --> GND

### 第三部分：比较电路

**1、使用的元器件**

|  元件   |       符号       | 型号 | 参数 | Proteus 元件库描述 |
|:-----:|:--------------:|:--:|:--:|:-------------:|
| LED 灯 |     D1，D2      | -- | -- |      --       |
|  电阻   | R5，R6，R7，R8，R9 | -- | -- |      --       |
|  电位器  |      RV2       | -- | -- |      --       |


LM393 引脚标号：
```text
Pin 1 -- Output A         (比较器 A 输出，开集电极)
Pin 2 -- Inverting A (-)  (比较器 A 反相输入)
Pin 3 -- Non-Inverting A  (比较器 A 同相输入)
Pin 4 -- GND
Pin 5 -- Non-Inverting B
Pin 6 -- Inverting B
Pin 7 -- Output B
Pin 8 -- VCC              (+12V)
```

**2、接线方式**
一、供电： Pin 8 → +12V，Pin 4 → GND

二、比较输入：
- Pin 2（反相输入−） → 接 运放 B 输出（Pin 7），即放大后的温度信号
- Pin 3（同相输入+） → 接 Vref 电位器中抽头（电位器两端接 +5V 和 GND，调节得到目标温度对应的参考电压）

三、滞回反馈（施密特触发器）：
- Pin 3 → R5(100kΩ) → Pin 1（输出）（正反馈，产生滞回）
- Pin 3 → R6(1MΩ) → GND（为同相端提供直流偏置）

四、输出上拉： LM393 是开集电极输出，需要上拉电阻才能输出高电平：
- Pin 1 → R7(10kΩ) → +12V

五、LED 指示：
- 绿色 LED（加热中，输出高电平时亮）：
  Pin 1 → 绿色 LED 阳极 → 绿色 LED 阴极 → R7(1kΩ) → GND
- 红色 LED（停止，输出低电平时亮）：
  +12V → R8(1kΩ) → 红色 LED 阳极 → 红色 LED 阴极 → Pin 1