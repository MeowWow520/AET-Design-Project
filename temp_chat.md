完整接线：
第一级（同相放大，增益 10）— 运放A：
- 电桥 Vout+（引脚A） → 直接接 Pin 3（运放A同相输入端）
- Pin 2（运放A反相输入端） → Rg1(1kΩ) → GND，同时 Pin 2 → Rf1(9kΩ) → Pin 1
- Pin 1（运放A输出） → 15.9kΩ 电阻 → 1μF 电容 → GND（RC 低通滤波）→ 输出给第二级，记为 V_stage1
  第二级（减法器，增益 10）— 运放B：
- 电桥 Vout−（引脚B） → Rg2(10kΩ) → Pin 5（运放B同相输入端）；同时 Pin 5 → 另一只 10kΩ 电阻 → GND
- 第一级输出 V_stage1 → Rg2(10kΩ) → Pin 6（运放B反相输入端）；同时 Pin 6 → Rf2(100kΩ) → Pin 7（运放B输出）
- Pin 7（运放B输出） → 送比较器 LM393 做滞回比较

第三级（滞回比较器 + LED 指示）— LM393（用 A 通道）：
- Pin 8（VCC）→ +12V，Pin 4（GND）→ GND
- Pin 2（运放B反相输入−）→ 接运放B输出（Pin 7），即放大后的温度信号
- Pin 3（运放B同相输入+）→ 接 Vref 电位器中抽头（电位器两端接 +5V 和 GND，调节得到目标温度对应的参考电压）
- 滞回反馈：
  - Pin 3 → R1(100kΩ) → Pin 1（比较器输出，正反馈）
  - Pin 3 → R2(1MΩ) → GND（直流偏置）
- 开集电极上拉：Pin 1 → 10kΩ → +12V
- LED 指示：
  - 绿色 LED（加热中，输出高电平时亮）：Pin 1 → 绿色 LED 阳极 → 绿色 LED 阴极 → 1kΩ → GND
  - 红色 LED（停止，输出低电平时亮）：+12V → 1kΩ → 红色 LED 阳极 → 红色 LED 阴极 → Pin 1

供电： Pin 8 → +12V，Pin 4 → GND